# Rosetta runtime.
#
# Pinned to the minor, not the patch: the GenAI conventions are still
# unreleased and attribute names have moved repeatedly, so the exporter and SDK
# versions used to verify this project are recorded deliberately.
opentelemetry-api~=1.44
opentelemetry-sdk~=1.44
opentelemetry-exporter-otlp-proto-http~=1.44

PyYAML>=6.0

# Tests only.
pytest>=8.0
